package com.example.artvswar.repository;

import com.example.artvswar.model.MockRoom;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MockRoomRepository extends JpaRepository<MockRoom, Long> {
}
